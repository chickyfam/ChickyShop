import { createAppContainer } from 'react-navigation';
import StackNavigator from 'ChickyShop/src/Navigators/StackNavigator';

const AppContainer = createAppContainer(StackNavigator);
export default AppContainer;
